# المراجعة المستقلة — تنفيذ ق40 (2026-09-24)

مراجعة مستقلة للإيداع `7167f06` (رفع المدير لإجراءات حلاق أُوقف حسابه وهو دون اتصال). كل بند أُعيد إنتاجه
باختبار قبل الإصلاح، ثم صار الاختبار دائمًا في ملفات الاختبار القائمة.

| البند | الخطورة | الوصف | الحالة |
|---|---|---|---|
| F1 | عالية | الرفع كان يقبل أحداثًا بوقت «تقريبي» (أُعيد تشغيل التطبيق أو الجهاز دون اتصال). هذا الوقت غير مصحّح بالساعة الرتيبة، فقد يُختم إجراء وقع **بعد** الإيقاف بوقت قبله فيُطبّق. وكان التطبيق عند إعادة فتحه يفقد الوقت الذي كان فيه مغلقًا (`restoreAnchor`). | ✅ (أ) في الرفع لا يُطبّق أي حدث تقريبي: النتيجة `rejected_uncertain_time` (`UNCERTAIN_TIME`)، ويُعدّ في الملخص (`rejectedUncertainTime`) ويظهر في ملخص شاشة الحجز بالعربية، ويُحفظ عنصرًا «لم يُطبّق — سجّله يدويًا إن حدث» في «إجراءات مستردة للمراجعة» بنوعه وحجزه وزبونه ووقت الجهاز وأهم بياناته (كالمبلغ). (ب) التطبيق يحفظ مع كل مزامنة قراءة «منذ تشغيل الجهاز» (`SystemClock.elapsedRealtime` وعدّاد التشغيل عبر قناة `saloni/boot_clock`، مجرّدة بـ`BootClock` قابلة للحقن)؛ إعادة فتح التطبيق دون إعادة تشغيل الجهاز ← وقت دقيق يحسب مدة الإغلاق (ونوم الجهاز أيضًا)؛ بعد إعادة التشغيل ← ساعة الجهاز مقيّدة بألا تسبق آخر وقت سيرفر، ويبقى تقريبيًا. |
| F2 | متوسطة | بعد إعادة التفعيل كانت المزامنة العادية تطبّق ما وقع أثناء الإيقاف دون أي علامة. | ✅ جدول `staff_suspensions` (ترحيل 007، مُلئ من `suspended_at` الحالي) يحفظ كل الفترات. في المزامنة العادية: الوقت الدقيق داخل فترة، أو الوقت التقريبي وفي الحساب فترة انتهت بعده ← يُطبّق (لا فقد بيانات) ويُعلَّم ويظهر في القائمة نفسها بنوع `during_suspension`. مسار `revokedAt` الخامل لم يُربط لأنه يرفض ما بعد الإيقاف (خلاف قرار «لا رفض»). |
| F3 | متوسطة | ورديات الحلاق تُقرأ للحسابات النشطة فقط، فكانت الاستراحة والغياب المستردّة تُرفض `NOT_WORKING`، وقفل يوم الحلاق الموقوف يستخدم اليوم كاملًا بدل ورديته. | ✅ دوال الوردية تقبل `includeInactive` ويمررها مسار المزامنة/الاسترداد؛ اختبار تكامل للأنواع الثلاثة (`break_started`، `break_ended`، `absent_today`). |
| F4 | منخفضة | تدقيق `staff.events_recovered` يُكتب بعد انتهاء الدفعة خارج أي معاملة — يضيع إن فشل الرفع في منتصفه. | ✅ يُكتب في `try/finally` بالنتائج المعروفة (`complete: false` عند الفشل)، وتدقيق `staff.event_recovered` لكل حدث داخل معاملته. |
| F5 | منخفضة | الرفع كان يرسل تنبيهات للمدير (`sync_conflict`، غياب الحلاق، مدة الخدمة الأساسية) رغم أن التوثيق يقول لا تنبيهات. | ✅ لا تنبيهات عند الاسترداد؛ صفوف التعارض والمراجعة تبقى. |
| F6 | منخفضة | حالة الحساب لا يُعاد فحصها داخل معاملة كل حدث؛ ورفع متزامن لحدث طُبّق يعود `ALREADY_STARTED` (أو خطأ داخلي) بدل `duplicate`. | ✅ `SELECT … FROM staff … FOR SHARE` داخل معاملة كل حدث (وتسجيل رفضه): الاسترداد يتطلب بقاء الإيقاف نفسه وإلا `409 ACCOUNT_NOT_SUSPENDED` للدفعة دون تسجيل الباقي؛ المزامنة العادية تتطلب النشاط وإلا `401`. بعد أخذ القفل يُعاد فحص معرّف الحدث ← `duplicate`. |
| R2 | — | غياب مسجّل قبل منتصف الليل خارج الوردية يقع على يوم ما بعد الإيقاف. | ليس عيبًا (سلوك «غائب اليوم» المعتاد: يوم العمل التشغيلي الحالي). |

## من فحص سريع لـ`deploy/`
- `edoburu/pgbouncer` مثبّت على `v1.25.2-p0` (PgBouncer 1.25.2؛ الملخص `sha256:7d7a27d9…` مطابق لما في Docker Hub) في `deploy/` و`infra/`. ✅
- `deploy/Caddyfile`: HSTS لسنة، `X-Content-Type-Options: nosniff`، `Referrer-Policy: no-referrer`، وحذف ترويسة `Server`؛ `caddy validate` ✅.

## بعد الإصلاح — الفحص الكامل
السيرفر 72 وحدة + 146 تكامل · `saloni_api` 95 · تطبيق الطاقم 49 · تطبيق الزبون 21 · التحليل نظيف (`dart analyze`، `flutter analyze` للتطبيقين) · بناء APK تطوير لتطبيق الطاقم ✅ · الاختبار الشامل `e2e/run.sh` ✅.

## متبقٍّ معروف
- iOS بلا ساعة تشغيل: بعد إعادة فتح التطبيق دون اتصال تبقى الأوقات تقريبية (فلا تُطبّق في الرفع، وتُحفظ للمراجعة).
- فترات الإيقاف المغلقة قبل ترحيل 007 غير معروفة (لم تُحفظ من قبل).
- ما زالت فحوص الحالة في آلة الحالات تقرأ صف الحجز قبل قفل اليوم؛ تسابق **حدثين مختلفين** على الحجز نفسه (لا الحدث نفسه) قد يعطي خطأ داخليًا بدل رفض مرتب — سابق لـق40 ولم يُغيَّر.
