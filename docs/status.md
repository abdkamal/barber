# حالة المشروع

**المرحلة الحالية: 11 — المستخدم يجرّب نسخة التطوير** (آخر تحديث: 2026-09-24)

| # | المرحلة | الحالة |
|---|---|---|
| 1 | التخطيط | ✅ المتطلبات والقرارات ق1–ق31 |
| 2 | اعتماد الخطة (مراجعة مستقلة) | ✅ اعتمد المستخدم المسودة 2 |
| 3 | تجهيز البيئة | ✅ (Android SDK مثبت بعد فتح `dl.google.com`) |
| 4 | تصميم الواجهات + النموذج الأولي | ✅ نظام التصميم والنموذج الأولي معتمدان |
| — | **بوابة: موافقة نصية صريحة على بدء البرمجة** | ✅ 2026-09-24 |
| 5 | تنفيذ الكود | ✅ كل المعالم + اختبار شامل من الطرف للطرف |
| 6 | مراجعة الكود (مستقلة) | ✅ صحة ومطابقة + أمان (اختراق فعلي) — `reviews/2026-09-24-phase6.md` |
| 7 | ربط الواجهات | ✅ التطبيقان على السيرفر الحقيقي (اختبار شامل) |
| 8 | كتابة الاختبارات | ✅ 451 اختبارًا |
| 9 | تشغيل الفحوص (بما فيها الأمنية) | ✅ كل الفحوص خضراء، `npm audit` نظيف |
| 10 | مراجعة النتائج (مستقلة) | ✅ تحقق مستقل + جولة إصلاح ثانية |
| 11 | تجربة نسخة التطوير من المستخدم | ⏳ السيرفر يعمل على خادم المستخدم `https://85-121-125-59.sslip.io` (خلف Caddy الموجود لديه)؛ سُلّم APK (profile، ليس إصدار تسليم) للتطبيقين؛ بانتظار ملاحظاته |
| — | **بوابة: اعتماد المستخدم لنسخة التطوير** | — |
| 12 | بناء الإصدار والتسليم + دليل الاستخدام | لم تبدأ |
| 13 | تنظيف المشروع | لم تبدأ |

## المنجز
- المتطلبات، سياسة العمل، القرارات ق1–ق35.
- التصميم التقني المسودة 2 — **معتمد**.
- تجهيز البيئة (`environment.md`).
- الاسم «صالوني — احجز دوري» (ق36) وملف الصالون (ق37)؛ التصميم 2.1.
- نظام التصميم (`design/design-system/`) باتجاه «ليل الخليج» (ق38) وشريط التقدم (ق39) — **معتمد**.
- النموذج الأولي التفاعلي (`design/prototype/`، 19 شاشة) — منشور للمراجعة.

## تقدم المرحلة 5
| المعلم | الحالة |
|---|---|
| 1. المحرك + المحاكي (`packages/engine`) | ✅ 40 اختبارًا ناجحًا |
| عقد الواجهة البرمجية (`docs/api.md`) | ✅ |
| 2. أساس السيرفر (`server`, `infra`) | ✅ 23 اختبار وحدة + 43 اختبار تكامل |
| 3. نظام التصميم في Flutter (`packages/saloni_ui`) | ✅ 45 اختبارًا |
| 4أ. عميل Dart والمزامنة (`packages/saloni_api`) | ✅ 37 اختبارًا |
| 4ب. منطق السيرفر (الحجز، المزامنة، التنبيهات، التقارير) | ✅ 53 اختبار وحدة + 96 اختبار تكامل |
| 5. تطبيق الطاقم (`apps/staff`) | ✅ 26 اختبارًا |
| 6. تطبيق الزبون (`apps/customer`) | ✅ 11 اختبارًا |
| 7. التكامل: النقل اليدوي (ق25)، طوابير المدير، مطابقة الحزم للسيرفر، الخطوط، اختبار شامل | ✅ |

## نتائج الفحص الكامل (2026-09-24)
| الجزء | التحليل | الاختبارات |
|---|---|---|
| المحرك | ✅ | 40 |
| السيرفر | ✅ | 53 وحدة + 105 تكامل |
| saloni_api | ✅ | 65 |
| saloni_ui (+ المثال) | ✅ | 61 + 5 |
| تطبيق الطاقم | ✅ | 28 |
| تطبيق الزبون | ✅ | 15 |
| اختبار شامل (سيرفر حقيقي + عميل حقيقي) | ✅ | تسجيل صالون ← تفعيل ← حجز ← نقل ← خدمة دون اتصال ← مزامنة ← دفع ← تقرير |

## جولة 2 من إصلاحات المرحلة 10 — التطبيقات والحزم (2026-09-24)
تنفَّذت بالتوازي مع إصلاحات `server/` لنفس المرحلة (انظر `docs/api.md` للتغييرات
المرتبطة: `unfinishedFromPreviousDay`، مهلة إعادة استخدام رمز التجديد).
| البند | الحزمة/التطبيق | الحالة |
|---|---|---|
| فقدان بيانات: خروج قسري كان يمسح الصندوق دائمًا | `apps/staff` (`state/app_services.dart`, `data/barber_repository.dart`) | ✅ يبقى الصندوق إلا عند تأكيد الإيقاف صراحة أو دخول حساب مختلف؛ إغلاق المقبض قبل أي مسح (لا مقبضان متزامنان) |
| L7 متبقٍّ: تسريب رمز تجديد صالون غير متذكَّر | `apps/customer` (`services/multi_salon_store.dart`) | ✅ `_persist` يكتب فقط الصالونات المتذكَّرة |
| I5 متبقٍّ: تثبيت «الأسرع» على صالون ليلي قد يعطي وقتًا ماضيًا | `packages/saloni_api` (`src/tz_util.dart`) | ✅ يلتف لأقرب وقوع قادم |
| عرض `unfinishedFromPreviousDay` في تطبيق الطاقم | `packages/saloni_api` (`StaffToday`)، `apps/staff` (شاشة «طابوري») | ✅ قسم «من يوم سابق» أعلى الشاشة بأزرار إنهاء/تأكيد دفع عبر الصندوق |

جميع الفحوص خضراء (`saloni_api` 76، `saloni_ui`+المثال 68، `apps/staff` 41،
`apps/customer` 21)، بناء الويب لكلا التطبيقين ناجح، واختبار الطرف للطرف
(`e2e/run.sh`) ناجح فوق السيرفر بعد إصلاحاته المتزامنة.

## المرحلة 11 — تنفيذ ق40 (2026-09-24)
| البند | الأجزاء | الحالة |
|---|---|---|
| ق40: إجراءات حلاق أُوقف حسابه وهو دون اتصال — يرفعها المدير (ما قبل الإيقاف فقط) ثم تُراجع | `server` (ترحيل 006، `sync/recovery.*`)، `packages/saloni_api`، `apps/staff` (شاشة «سلّم الجهاز للمدير»، «إجراءات مستردة للمراجعة») | ✅ السيرفر 69 وحدة + 140 تكامل، `saloni_api` 84، `apps/staff` 45، التحليل نظيف، اختبار الطرف للطرف ناجح |
| إصلاحات المراجعة المستقلة لـق40 (F1–F6: الوقت غير المؤكد لا يُطبّق + ساعة التشغيل في التطبيق، فترات الإيقاف وعلامة «أثناء الإيقاف»، ورديات الحساب الموقوف، التدقيق، التنبيهات، إعادة الفحص داخل المعاملة) + تثبيت إصدار PgBouncer وترويسات أمان Caddy — `docs/reviews/2026-09-24-q40.md` | `server` (ترحيل 007)، `packages/saloni_api`، `apps/staff` (قناة أندرويد `saloni/boot_clock`)، `deploy/`، `infra/` | ✅ السيرفر 72 وحدة + 146 تكامل، `saloni_api` 95، `apps/staff` 49، `apps/customer` 21، التحليل نظيف، APK تطوير يُبنى، اختبار الطرف للطرف ناجح |

## المرحلة 11 — ملاحظات التجربة الأولى (2026-09-24)
ملاحظات المستخدم بعد تجربة نسخة التطوير على هاتفه (القرار ق41). جزء التسجيل والإعدادات والتنسيق:
| الملاحظة | الإصلاح | الأجزاء | الحالة |
|---|---|---|---|
| إضافة الشيكل دون علم إسرائيل | `ILS` (₪) ضمن العملات، ولا أعلام إطلاقًا: رمز كل عملة علامتها في القائمة؛ تعريف العملات صار مشتركًا (`SaloniCurrency`)؛ اختيار «بلد الصالون (التوقيت)» عند التسجيل (يُقترح من العملة) | `packages/saloni_api` (`currency.dart`)، `apps/staff` (التسجيل، `format.dart`)، `server` (اختبار قبول ILS والمناطق) | ✅ |
| منتقي الساعة: اختيار 9 يحفظ 10 | السبب: عرض الساعات الجدارية كان يبني الوقت بتوقيت **الهاتف** ثم يحوّله لتوقيت الصالون (فرق ساعة بين فلسطين والرياض الافتراضي). صارت تُعرض كما اختيرت؛ وفترات «يوم محدد» تُحسب بتوقيت الصالون. اختبارات انحدار | `apps/staff` (`displayWireTime`/`wallTimeAr`، الدوام) + `salonWallTimeToUtc` | ✅ |
| واتساب ببادئة ‎+966 ثابتة | رقم دولي كامل بأي رمز دولة (E.164) مع تحقق وشرح؛ السيرفر يرفض الرقم المحلي بلا رمز دولة | `apps/staff` (ملف الصالون)، `server` (`normalizeInternationalPhone`) | ✅ (تطبيق الزبون لم يُعدَّل بطلب المستخدم) |
| إلغاء الأرقام المشرقية | حُذف الخيار من «المزيد» والإعدادات ويُمسح تفضيله القديم؛ `digits()` تحوّل لغربية دائمًا؛ `DateFormat` لا يستخدم الأرقام المشرقية للعربية | `apps/staff` (`format.dart`، `prefs.dart`، `main.dart`) | ✅ |
| شرح الخيارات غير الواضحة | مكوّن `SaloniHelpHint` (ⓘ ← ورقة شرح + مثال) + سطر مساعد؛ 32 نصًا في `help_texts.dart` للإعدادات والتسجيل والدوام والخدمات والطاقم والزبائن والتأجيل | `packages/saloni_ui`، `apps/staff` | ✅ |

جزء الشاشات التشغيلية (زبون حاضر، QR، «لن أعمل اليوم»، الاستراحة، التنقل):
| الملاحظة | الإصلاح | الأجزاء | الحالة |
|---|---|---|---|
| «زبون حاضر»: خطأ «اختر خدمة واحدة على الأقل» ولا منتقي خدمات ظاهر | قسم «الخدمات» بشرائح خدمات الصالون (اختيار متعدد، المدة والسعر، مجموع المدة والسعر)؛ خدمة واحدة تُختار تلقائيًا؛ إن لم تصل الخدمات بعد تُطلب من السيرفر مباشرة، وبلا خدمات تنبيه «لا خدمات متاحة» مع إعادة تحميل والزر معطل؛ رسالة الخطأ تشير إلى «الخدمات» الظاهرة؛ الإضافة لا ترفض «دون اتصال» خطأً قبل أول نبضة | `apps/staff` (`walkin_screen.dart`، `barber_repository.dart`) | ✅ |
| زر QR في الشريط العلوي | شريط علوي دائم في هيكلي الحلاق والمدير: اسم الصالون + زر QR يفتح ورقة فيها رمز QR (قيمته رمز الصالون كما يقرؤه تطبيق الزبون) والرمز نصًا وزر «نسخ الرمز» (المشاركة عبر النسخ — بلا حزمة جديدة) | `apps/staff` (`common/salon_qr.dart`، `shells.dart`) | ✅ |
| «لن أعمل اليوم»: لا استجابة فورية، ولا تراجع، و«راجع مديرك» حتى للمدير | تأكيد قبل التنفيذ ثم تغيّر فوري (متفائل، يعمل دون اتصال) مع رسالة؛ خطأ التسجيل المحلي يظهر بدل أن يُتجاهل. **تراجع**: حدث جديد `absent_cancelled` عبر آلة الحالات (قفل اليوم، تكرار آمن، تدقيق، تنبيه المديرين `barber_absence_cancelled` حين يتراجع حلاق)؛ المدير يتراجع دائمًا (ليومه بالحدث، ولأي حلاق من «الطوابير» ← «إلغاء الغياب»)؛ الحلاق عن غياب سجّله بنفسه فقط (`ABSENCE_SET_BY_MANAGER` وإلا). نص التأكيد يوضح أن ما نقله المدير لا يعود تلقائيًا. `day.absence` في `GET /staff/today` | `server` (`sync.service.ts`، `staff-day.service.ts`، `absences.controller.ts`، `reasons.ts`، التنبيهات)، `packages/saloni_api`، `apps/staff` (`breaks_screen.dart`، `queue_screen.dart`، `queues_screen.dart`) | ✅ |
| «استراحة طارئة الآن» متاحة رغم «لن أعمل اليوم» | معطلة مع شرح (والاستراحات المجدولة كذلك)؛ السيرفر يرفض `break_started` في يوم غياب بـ`BARBER_ABSENT` | `apps/staff`، `server` | ✅ |
| «طابوري» يصعب الرجوع منه | السبب: `go('/b/queue')` ينقل المدير إلى هيكل الحلاق بلا مكدّس، وزر أندرويد يخرج من التطبيق. الإصلاح: شريط علوي في «وضع الحلاق» للمدير بزر «لوحة المدير» (السهم يمينًا في RTL)؛ رجوع أندرويد على مستوى الهياكل (من أي تبويب إلى الأول، ومن «طابوري» للمدير إلى لوحته)؛ الشاشات المستقلة ترجع لمكدّسها أو — إن فُتحت مباشرة — إلى «طابوري»/الإعدادات بدل شاشة البداية | `apps/staff` (`shells.dart`) | ✅ |

الفحوص: السيرفر 76 وحدة + 148 تكامل (منها 2 جديدان)، `saloni_api` 105، `apps/staff` 66 (منها 14 جديدًا في `trial_feedback_test.dart`)، `apps/customer` 21 دون تعديل، التحليل نظيف، اختبار الطرف للطرف ناجح.

الجولة 2 (عن التطبيق، المدير الذي يحلق هدفًا للنقل، أخطاء «حدث خطأ غير متوقع»):
| الملاحظة | السبب الجذري / الإصلاح | الأجزاء | الحالة |
|---|---|---|---|
| «تعذر التسجيل: حدث خطأ غير متوقع» (و«غير متوقع» في إجراءات الحلاق) — لا شيء في سجل السيرفر | **السبب: قاعدة الجهاز المحلية لم تكن تعمل على الهاتف إطلاقًا.** القاعدة تعمل في عزلة خلفية (`createInBackground`) وتوجيه `sqlite3` إلى `libsqlcipher.so` كان يُطبَّق في العزلة الرئيسية فقط، فتبحث العزلة الخلفية عن `libsqlite3.so` غير الموجود في الـAPK (تحقّقنا: فيه `libsqlcipher.so` فقط) ← يفشل كل تسجيل محلي (غياب، استراحة، بدء/إنهاء، دفع) بخطأ Dart ليس `ApiError` فيظهر عامًا. الإصلاح: التوجيه داخل العزلة (`isolateSetup`) + التحقق من `PRAGMA cipher_version` (لا قاعدة غير مشفّرة بصمت). **يلزم تأكيده على الهاتف بـAPK جديد** | `packages/saloni_api` (`flutter.dart`) | ✅ (بانتظار تجربة الجهاز) |
| رسائل الأخطاء غير مفيدة | نص عربي لكل رمز خطأ في السيرفر وكل سبب رفض مزامنة (`core/error_texts.dart`)؛ أخطاء التحقق تذكر الحقل والمشكلة؛ الشبكة: «لا اتصال بالإنترنت» / «تعذّر الوصول للسيرفر» / «انتهت المهلة» / «السيرفر لا يستجيب (502/503/504)»؛ غير المتوقع: «(رمز: S-xxxxxx)» من معرّف طلب السيرفر أو «(رمز: C-<النوع>)» لخطأ داخل التطبيق، وخطأ قاعدة الجهاز برسالة خاصة؛ الأصل ومكدّسه يُطبعان في السجل دائمًا (27 موضع `catch` صار يمرّر المكدّس؛ لا ابتلاع صامت في تحميل القاعدة/الطابور) | `apps/staff`، `packages/saloni_api` (`ApiError.requestId`/`shortRequestId`/`validationIssues`/`networkFailure` — إضافات لا تغيّر سلوك تطبيق الزبون) | ✅ |
| لا سجلات قابلة للتتبع في السيرفر | `X-Request-Id` لكل طلب (يُولَّد أو يُحفظ الوارد)؛ الخطأ غير المعالَج يُسجَّل بالمعرّف والمسار النمطي والمكدّس (بلا أجسام/استعلامات/ترويسات) ويعود `500 {code: INTERNAL, requestId}`؛ أخطاء 4xx المعالَجة بسطر واحد (ولأخطاء التحقق المسارات فقط) | `server` (`common/request-id.ts`، `http-exception.filter.ts`، `bootstrap.ts`) | ✅ |
| تدقيق التسجيل وكل عمليات المدير/الحلاق بحثًا عن 500 | اختبار تكامل يسجّل صالونًا بكل منطقة وعملة يعرضها التطبيق (11 منطقة، 9 عملات منها ILS) ثم خطوات التسجيل التالية وواتساب دولي؛ واختبار طرف-لطرف جديد (`e2e/test/staff_app_contract_test.dart`) يمرّر **كل** نداء لتطبيق الطاقم عبر `ApiClient` الحقيقي بأجسام الشاشات نفسها على سيرفر حقيقي. **لم يظهر أي 500 ولا عدم تطابق في النماذج.** وُجد: تطبيق التسجيل كان يقبل اسم مستخدم يبدأ بـ`.`/`_`/`-` فيرفضه السيرفر برسالة عامة — وُحّد مع السيرفر؛ ومسح قاعدة الجهاز بعد تسجيل ناجح صار أفضل جهد (لا «غير متوقع» بعد إنشاء الصالون فعلًا) | `server/test/trial-feedback.int-spec.ts`، `e2e`، `apps/staff` (التسجيل، `app_services.dart`) | ✅ |
| النقل اليدوي (ق25) لا يعرض المدير | السيرفر متّسق: يُحجز عند الموظف ويُنقل إليه متى كان له دوام اليوم، و**دوام الصالون الافتراضي يسري على الحلاقين فقط** — فالمدير بلا دوام خاص لا يُحجز عنده ولا يظهر في الطوابير ولا في ورقة النقل (فلا تتأثر صالونات مالكها لا يحلق). الإصلاح في التطبيق: ورقة النقل تعرض كل الطاقم النشط، ومن لا دوام له غير متاح مع السبب، والمدير منهم بزر «اعمل بدوام الصالون» (ينسخ الافتراضي لأيامه دون تغيير ما ضبطه) فيصبح هدفًا فورًا؛ الطوابير تذكر من لا دوام له؛ «طابوري» للمدير بلا دوام يشرح ويعرض الزر نفسه؛ شرح «لمن؟» في الدوام يذكر المدير. تحقّق السيرفر: المدير بدوام خاص يظهر للزبائن وفي الطوابير ويُنقل إليه ومنه ويضيف زبونًا حاضرًا؛ وبلا دوام `409 BARBER_NOT_WORKING`. التقارير لا تفلتر بالدور (المدير يظهر بحجوزاته) | `apps/staff` (`queues_screen.dart`، `manager_barber.dart`، `queue_screen.dart`، `help_texts.dart`)، `server/test` | ✅ — **قرار للمستخدم:** هل يسري دوام الصالون على المديرين تلقائيًا؟ (الآن: لا، بنقرة) |
| «عن التطبيق» | شاشة من «المزيد» (الحلاق) ومن إعدادات المدير: «صالوني — احجز دوري»، الإصدار (ثابت في `AppConfig.appVersion` — لا حزمة package_info)، «شركة فينكس لتطوير الأنظمة والحلول البرمجية»، «برمجة م. عبدالرحمن أبوعون»، هاتف 0598789755 (معزول LTR، قابل للنقر ← `tel:` عبر `url_launcher` المضاف لتطبيق الطاقم + استعلام `DIAL` في المانيفست) | `apps/staff` (`about_screen.dart`، `app.dart`، `more_screen.dart`، `config.dart`، `pubspec.yaml`) | ✅ |

الفحوص: السيرفر 80 وحدة (4 جديدة) + 165 تكامل (17 جديدًا)، `saloni_api` 110 (5 جديدة)، `saloni_ui` 68، `apps/staff` 79 (13 جديدًا في `trial_feedback_2_test.dart`)، `apps/customer` 21 دون تعديل، التحليل نظيف، الطرف للطرف 2 (منها عقد تطبيق الطاقم الجديد) ناجح. لم يُبنَ APK جديد بعد.

## المتبقي
1. المراجعة المستقلة للكود (المرحلة 6) والفحص الأمني.
2. ~~قرار المستخدم: أحداث حساب حلاق أُوقف أثناء الانقطاع~~ — حُسم (ق40) ونُفّذ.
3. ✅ حُل حجب `dl.google.com` وبُني APK للتطبيقين. المتبقي: تجربة SQLCipher (أُصلح سبب فشلها على الجهاز في الجولة 2 — يلزم APK جديد) والخدمة الأمامية على جهاز المستخدم (FCM مؤجل — لا مشروع Firebase).
4. معروفة ومؤجلة: WebSocket في تطبيق الطاقم (المزامنة كل 30 ث + فورًا بعد كل إجراء)، عرض الأوقات بتوقيت الصالون لا الجهاز.
