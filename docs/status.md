# حالة المشروع

**المرحلة الحالية: 11 — بانتظار تثبيت السيرفر على خادم المستخدم** (آخر تحديث: 2026-09-24)

| # | المرحلة | الحالة |
|---|---|---|
| 1 | التخطيط | ✅ المتطلبات والقرارات ق1–ق31 |
| 2 | اعتماد الخطة (مراجعة مستقلة) | ✅ اعتمد المستخدم المسودة 2 |
| 3 | تجهيز البيئة | ✅ (Android SDK مثبت بعد فتح `dl.google.com`) |
| 4 | تصميم الواجهات + النموذج الأولي | ✅ نظام التصميم والنموذج الأولي معتمدان |
| — | **بوابة: موافقة نصية صريحة على بدء البرمجة** | ✅ 2026-09-24 |
| 5 | تنفيذ الكود | ✅ كل المعالم + اختبار شامل من الطرف للطرف |
| 6 | مراجعة الكود (مستقلة) | ✅ صحة ومطابقة + أمان (اختراق فعلي) — `reviews/2026-09-24-phase6.md` |
| 7 | ربط الواجهات | ✅ التطبيقان على السيرفر الحقيقي (اختبار شامل) |
| 8 | كتابة الاختبارات | ✅ 451 اختبارًا |
| 9 | تشغيل الفحوص (بما فيها الأمنية) | ✅ كل الفحوص خضراء، `npm audit` نظيف |
| 10 | مراجعة النتائج (مستقلة) | ✅ تحقق مستقل + جولة إصلاح ثانية |
| 11 | تجربة نسخة التطوير من المستخدم | ⏳ حزمة النشر `deploy/` جاهزة، بناء APK (debug) ناجح؛ بانتظار تثبيت المستخدم على خادمه وإرسال العنوان. لا Firebase: المتابعة بالتحديث الدوري |
| — | **بوابة: اعتماد المستخدم لنسخة التطوير** | — |
| 12 | بناء الإصدار والتسليم + دليل الاستخدام | لم تبدأ |
| 13 | تنظيف المشروع | لم تبدأ |

## المنجز
- المتطلبات، سياسة العمل، القرارات ق1–ق35.
- التصميم التقني المسودة 2 — **معتمد**.
- تجهيز البيئة (`environment.md`).
- الاسم «صالوني — احجز دوري» (ق36) وملف الصالون (ق37)؛ التصميم 2.1.
- نظام التصميم (`design/design-system/`) باتجاه «ليل الخليج» (ق38) وشريط التقدم (ق39) — **معتمد**.
- النموذج الأولي التفاعلي (`design/prototype/`، 19 شاشة) — منشور للمراجعة.

## تقدم المرحلة 5
| المعلم | الحالة |
|---|---|
| 1. المحرك + المحاكي (`packages/engine`) | ✅ 40 اختبارًا ناجحًا |
| عقد الواجهة البرمجية (`docs/api.md`) | ✅ |
| 2. أساس السيرفر (`server`, `infra`) | ✅ 23 اختبار وحدة + 43 اختبار تكامل |
| 3. نظام التصميم في Flutter (`packages/saloni_ui`) | ✅ 45 اختبارًا |
| 4أ. عميل Dart والمزامنة (`packages/saloni_api`) | ✅ 37 اختبارًا |
| 4ب. منطق السيرفر (الحجز، المزامنة، التنبيهات، التقارير) | ✅ 53 اختبار وحدة + 96 اختبار تكامل |
| 5. تطبيق الطاقم (`apps/staff`) | ✅ 26 اختبارًا |
| 6. تطبيق الزبون (`apps/customer`) | ✅ 11 اختبارًا |
| 7. التكامل: النقل اليدوي (ق25)، طوابير المدير، مطابقة الحزم للسيرفر، الخطوط، اختبار شامل | ✅ |

## نتائج الفحص الكامل (2026-09-24)
| الجزء | التحليل | الاختبارات |
|---|---|---|
| المحرك | ✅ | 40 |
| السيرفر | ✅ | 53 وحدة + 105 تكامل |
| saloni_api | ✅ | 65 |
| saloni_ui (+ المثال) | ✅ | 61 + 5 |
| تطبيق الطاقم | ✅ | 28 |
| تطبيق الزبون | ✅ | 15 |
| اختبار شامل (سيرفر حقيقي + عميل حقيقي) | ✅ | تسجيل صالون ← تفعيل ← حجز ← نقل ← خدمة دون اتصال ← مزامنة ← دفع ← تقرير |

## جولة 2 من إصلاحات المرحلة 10 — التطبيقات والحزم (2026-09-24)
تنفَّذت بالتوازي مع إصلاحات `server/` لنفس المرحلة (انظر `docs/api.md` للتغييرات
المرتبطة: `unfinishedFromPreviousDay`، مهلة إعادة استخدام رمز التجديد).
| البند | الحزمة/التطبيق | الحالة |
|---|---|---|
| فقدان بيانات: خروج قسري كان يمسح الصندوق دائمًا | `apps/staff` (`state/app_services.dart`, `data/barber_repository.dart`) | ✅ يبقى الصندوق إلا عند تأكيد الإيقاف صراحة أو دخول حساب مختلف؛ إغلاق المقبض قبل أي مسح (لا مقبضان متزامنان) |
| L7 متبقٍّ: تسريب رمز تجديد صالون غير متذكَّر | `apps/customer` (`services/multi_salon_store.dart`) | ✅ `_persist` يكتب فقط الصالونات المتذكَّرة |
| I5 متبقٍّ: تثبيت «الأسرع» على صالون ليلي قد يعطي وقتًا ماضيًا | `packages/saloni_api` (`src/tz_util.dart`) | ✅ يلتف لأقرب وقوع قادم |
| عرض `unfinishedFromPreviousDay` في تطبيق الطاقم | `packages/saloni_api` (`StaffToday`)، `apps/staff` (شاشة «طابوري») | ✅ قسم «من يوم سابق» أعلى الشاشة بأزرار إنهاء/تأكيد دفع عبر الصندوق |

جميع الفحوص خضراء (`saloni_api` 76، `saloni_ui`+المثال 68، `apps/staff` 41،
`apps/customer` 21)، بناء الويب لكلا التطبيقين ناجح، واختبار الطرف للطرف
(`e2e/run.sh`) ناجح فوق السيرفر بعد إصلاحاته المتزامنة.

## المرحلة 11 — تنفيذ ق40 (2026-09-24)
| البند | الأجزاء | الحالة |
|---|---|---|
| ق40: إجراءات حلاق أُوقف حسابه وهو دون اتصال — يرفعها المدير (ما قبل الإيقاف فقط) ثم تُراجع | `server` (ترحيل 006، `sync/recovery.*`)، `packages/saloni_api`، `apps/staff` (شاشة «سلّم الجهاز للمدير»، «إجراءات مستردة للمراجعة») | ✅ السيرفر 69 وحدة + 140 تكامل، `saloni_api` 84، `apps/staff` 45، التحليل نظيف، اختبار الطرف للطرف ناجح |
| إصلاحات المراجعة المستقلة لـق40 (F1–F6: الوقت غير المؤكد لا يُطبّق + ساعة التشغيل في التطبيق، فترات الإيقاف وعلامة «أثناء الإيقاف»، ورديات الحساب الموقوف، التدقيق، التنبيهات، إعادة الفحص داخل المعاملة) + تثبيت إصدار PgBouncer وترويسات أمان Caddy — `docs/reviews/2026-09-24-q40.md` | `server` (ترحيل 007)، `packages/saloni_api`، `apps/staff` (قناة أندرويد `saloni/boot_clock`)، `deploy/`، `infra/` | ✅ السيرفر 72 وحدة + 146 تكامل، `saloni_api` 95، `apps/staff` 49، `apps/customer` 21، التحليل نظيف، APK تطوير يُبنى، اختبار الطرف للطرف ناجح |

## المتبقي
1. المراجعة المستقلة للكود (المرحلة 6) والفحص الأمني.
2. ~~قرار المستخدم: أحداث حساب حلاق أُوقف أثناء الانقطاع~~ — حُسم (ق40) ونُفّذ.
3. ✅ حُل حجب `dl.google.com` وبُني APK للتطبيقين. المتبقي: تجربة SQLCipher والخدمة الأمامية على جهاز المستخدم (FCM مؤجل — لا مشروع Firebase).
4. معروفة ومؤجلة: WebSocket في تطبيق الطاقم (المزامنة كل 30 ث + فورًا بعد كل إجراء)، عرض الأوقات بتوقيت الصالون لا الجهاز.
