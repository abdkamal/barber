# Banner

رسالة ضمن الصفحة. يمرر المستهلك `tone` (`info`/`success`/`warning`/`danger`) و`title` و`children` واختياريًا `action`.

- للتنويهات والتعليمات داخل التطبيق (السياسة، المبدأ 11) وتحذير الإشعارات المعطلة.
- `danger` للأخطاء التي تمنع المتابعة فقط.
