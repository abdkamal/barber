# ContactBar

العنوان وأزرار التواصل في صفحة الصالون. يمرر المستهلك `address` واختياريًا `phone` و`whatsapp` و`maps` و`instagram` — يظهر فقط ما أدخله المدير.

- «الاتجاهات» يفتح خرائط Google بموقع الصالون. لا تتبع لموقع الزبون.
