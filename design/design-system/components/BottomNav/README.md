# BottomNav

الشريط السفلي، من 3 إلى 5 عناصر. يمرر المستهلك `items` (`icon`, `label`, `badge`) و`active`.

- الزبون: حجزي، الصالون، السجل، حسابي. الحلاق: طابوري، الدفعات، استراحاتي، المزيد. المدير يضيف: التقارير.
