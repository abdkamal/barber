# SegmentedControl

اختيار واحد من خيارين أو ثلاثة متجاورين. يمرر المستهلك `options` (`value`, `label`, `icon`) و`value` و`onChange` و`label` (لقارئ الشاشة).

- في الحجز: «أقرب دور» / «ساعة محددة». في التقارير: يوم / أسبوع / شهر.
- أكثر من ثلاثة خيارات ← قائمة، لا هذا المكوّن.
