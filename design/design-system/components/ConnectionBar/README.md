# ConnectionBar

شريط حالة الاتصال أعلى تطبيق الحلاق، ظاهر دائمًا. يمرر المستهلك `state` (`online`/`syncing`/`offline`) و`since` و`pending`.

- عند الانقطاع يوضح أن إضافة زبون حاضر متوقفة (ق3) وعدد الإجراءات المنتظرة للمزامنة.
