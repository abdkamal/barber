# ServiceChip

خدمة قابلة للاختيار في شاشة الحجز؛ يمكن اختيار أكثر من خدمة. يمرر المستهلك `name` و`minutes` و`price` و`currency` و`selected` و`onToggle`.

- المدة المعروضة هي المدة الأساسية للخدمة؛ التقدير الشخصي يظهر لاحقًا في التأكيد.
- السعر بأرقام جدولية وبعملة الصالون (ق34).
