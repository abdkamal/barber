# StatTile

رقم رئيسي في تقارير المدير. يمرر المستهلك `label` و`value` و`unit` واختياريًا `delta` و`deltaTone` و`series` (أعمدة صغيرة، آخرها مميز).

- الرقم يسبق التفاصيل؛ اللون الدلالي للتغير فقط لا للزينة.
