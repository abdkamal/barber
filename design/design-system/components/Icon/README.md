# Icon

أيقونات Phosphor بوزن regular، مضمّنة كمسارات SVG بلون `currentColor`. يمرر المستهلك `name` و`size` (افتراضي 20) واختياريًا `label` (إن حملت معنى بلا نص) و`mirror`.

- `mirror` للأيقونات الاتجاهية (الأسهم) حتى تنعكس في RTL.
- في Flutter: حزمة `phosphor_flutter` بالأسماء نفسها.
